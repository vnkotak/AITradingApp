/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
      appDir: true, // using the /app directory
    },
  }
  
  module.exports = nextConfig